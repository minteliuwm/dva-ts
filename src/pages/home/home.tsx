import * as React from 'react';
import { Button } from 'antd';

const Home = () => {
	return (
		<div className='home'>
			<Button type="primary">Button</Button>
		</div>
	);
};

export default Home;
